{
    "name": "money-streaming-intro",
    "scripts": {
        "install": "curl -L https://foundry.paradigm.xyz | bash",
        "test": "foundryup && forge install && forge test",
        "build": "forge build"
    }
}
