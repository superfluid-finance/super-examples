{
    "name": "money-streaming-intro",
    "scripts": {
        "install": "curl -L https://foundry.paradigm.xyz | bash && foundryup && forge install",
        "test": "forge test",
        "build": "forge build"
    }
}
